<h1>Menus</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
	  <th>Category</th>

    </tr>
  </thead>
  <tbody>
    <% @menus.each do |menu| %>
      <tr>
        <td><%= menu.name %></td>
		<td><%= number_to_currency(menu.price, :unit => "£")  %></td>
		<td><%= menu.category_name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
