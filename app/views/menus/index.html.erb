<h1>Menus</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price (£)</th>
	  <th>Category</th>

    </tr>
  </thead>
  <tbody>
    <% @menus.each do |menu| %>
      <tr>
        <td><%= menu.name %></td>
		<td><%= menu.price %></td>
		<td><%= menu.category_name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
