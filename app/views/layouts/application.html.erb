

<!DOCTYPE html>
<html>
<head>
  <title>Lunch Order</title>
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
		<script type="text/javascript">
		$(document).ready(function() {
		$('#select').change(function() {
			if($(this).val() == "Part-Time Staff")
		        $('#extra').show();
		    else
		        $('#extra').hide();

		});
	});
		</script>
</head>
<body>
  <div class="navbar navbar-fixed-top" style="color: blue;">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" <%= link_to t('.title'), menus_path %></a>
        <div class="nav-collapse">
          <ul class="nav">	
	
	            <li><%= link_to t('.menus'), menus_path %></li>
		  	
			<% if current_user %>
			  	<li><%= link_to t('.order_history'), orders_path %></li>
				<li><%= link_to t('.new_order'), new_order_path %></li>
			<% end %>
			<% if current_user && current_user.kind == "Part-Time Staff" %>
				<li><%= link_to t('.work_records'), work_records_path %></li>
				<li><%= link_to t('.attendance'), new_work_record_path %></li>
			<% end %>
          </ul>
		<% if current_user %>
		<ul class="nav pull-right">               
	        <li class="divider-vertical"></li>
	        <li style="color:lightblue;margin-top:10px;"><%= current_user.email %></li>
	        <li class="divider-vertical"></li>
	        <li><%= link_to t('.logout'), logout_path %></li>
	 		<li class="divider-vertical"></li>
			<li> <a class="dropdown-toggle" id="dLabel" role="button" data-toggle="dropdown" data-target="#" href="/page.html">
			        <%=  t('.language') %>
			        <b class="caret"></b>
			    </a>
			    <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
			        <li><a href="<%= request.path %>?locale=en">English</a></li>
			        <li><a href="<%= request.path %>?locale=jp">Japanese</a></li>
			    </ul></li>
	      </ul>
	<% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="container">

	
	

	<% flash.each do |name, msg| %>
	  <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
	    <a class="close" data-dismiss="alert">×</a>
	    <%= msg %>
	  </div>
	<% end %>


    <%= yield %>
</div>

<br/><center>	&copy; <%= Date.today %>  Japan Centre Group Ltd.</center>
</div>
</body>
</html>
