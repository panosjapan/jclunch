<%= form_tag password_resets_path, :method => :post do %>
  <div class="field">
    <%= label_tag :email %>
    <%= text_field_tag :email, params[:email] %>
  </div>
 <button class="btn btn-primary" type="submit"><%= (t "Reset Password") %></button>
<% end %>