<h1>Region</h1>

<p>
  <b>Name</b><br>
  <%= @region.name %>
</p>


<div class="form-actions">
  <%= link_to 'Back', admin_regions_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_admin_region_path(@region), :class => 'btn' %>
  <%= link_to 'Delete', admin_region_path(@region), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
